export default function (scene: BABYLON.Scene): void {  const cubeProperties: { [p: string]: number } = ({ height: 2, width: 2, depth: 2 });  const boxes: BABYLON.Mesh[] = [    BABYLON.MeshBuilder.CreateBox("box1", cubeProperties, scene),    BABYLON.MeshBuilder.CreateBox("box2", cubeProperties, scene),    BABYLON.MeshBuilder.CreateBox("box3", cubeProperties, scene),  ];  let material: BABYLON.StandardMaterial;  for (let i = 0; i < 3; i++) {    material = new BABYLON.StandardMaterial('standard', scene);    material.diffuseColor = new BABYLON.Color3(i % 2, (i + 1) % 2, i % 2);    boxes[i].material = material;    boxes[i].position = new BABYLON.Vector3(7, 7, -i*2);  }}